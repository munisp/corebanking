FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY tests/requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy test files
COPY tests/ ./tests/

CMD ["python", "-m", "pytest", "tests/smoke/", "-v", "--tb=short", "--no-header"]
