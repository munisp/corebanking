apiVersion: v1
kind: Service
metadata:
  name: {{ include "treasury-investments.fullname" . }}
  labels:
    {{- include "treasury-investments.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "treasury-investments.selectorLabels" . | nindent 4 }}
