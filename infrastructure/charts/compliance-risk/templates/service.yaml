apiVersion: v1
kind: Service
metadata:
  name: {{ include "compliance-risk.fullname" . }}
  labels:
    {{- include "compliance-risk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "compliance-risk.selectorLabels" . | nindent 4 }}
