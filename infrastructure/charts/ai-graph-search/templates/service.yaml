apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-graph-search.fullname" . }}
  labels:
    {{- include "ai-graph-search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "ai-graph-search.selectorLabels" . | nindent 4 }}
