{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "insurance-service.serviceAccountName" . }}
  labels:
    {{- include "insurance-service.labels" . | nindent 4 }}
{{- end }}
