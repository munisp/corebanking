apiVersion: v1
kind: Service
metadata:
  name: {{ include "atm-management-go.fullname" . }}
  labels:
    {{- include "atm-management-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "atm-management-go.selectorLabels" . | nindent 4 }}
