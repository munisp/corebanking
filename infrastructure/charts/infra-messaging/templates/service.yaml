apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-messaging.fullname" . }}
  labels:
    {{- include "infra-messaging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "infra-messaging.selectorLabels" . | nindent 4 }}
