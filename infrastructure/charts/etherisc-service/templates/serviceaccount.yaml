{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "etherisc-service.serviceAccountName" . }}
  labels:
    {{- include "etherisc-service.labels" . | nindent 4 }}
{{- end }}
