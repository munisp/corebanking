apiVersion: v1
kind: Service
metadata:
  name: {{ include "eod-processor-go.fullname" . }}
  labels:
    {{- include "eod-processor-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "eod-processor-go.selectorLabels" . | nindent 4 }}
