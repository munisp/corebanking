apiVersion: v1
kind: Secret
metadata:
  name: {{ include "workflow-operations.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
