apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflow-operations.fullname" . }}
  labels:
    {{- include "workflow-operations.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "workflow-operations.selectorLabels" . | nindent 4 }}
