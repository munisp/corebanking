apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-deposits.fullname" . }}
  labels:
    {{- include "core-deposits.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "core-deposits.selectorLabels" . | nindent 4 }}
