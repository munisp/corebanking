apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-cards.fullname" . }}
  labels:
    {{- include "core-cards.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "core-cards.selectorLabels" . | nindent 4 }}
