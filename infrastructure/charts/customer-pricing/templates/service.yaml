apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-pricing.fullname" . }}
  labels:
    {{- include "customer-pricing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "customer-pricing.selectorLabels" . | nindent 4 }}
