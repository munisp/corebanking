apiVersion: v1
kind: Service
metadata:
  name: {{ include "trade-fx.fullname" . }}
  labels:
    {{- include "trade-fx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "trade-fx.selectorLabels" . | nindent 4 }}
