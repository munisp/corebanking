{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "verification-service.serviceAccountName" . }}
  labels:
    {{- include "verification-service.labels" . | nindent 4 }}
{{- end }}
