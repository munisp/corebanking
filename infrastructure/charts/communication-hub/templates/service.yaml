apiVersion: v1
kind: Service
metadata:
  name: {{ include "communication-hub.fullname" . }}
  labels:
    {{- include "communication-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "communication-hub.selectorLabels" . | nindent 4 }}
