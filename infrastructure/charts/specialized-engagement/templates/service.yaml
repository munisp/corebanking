apiVersion: v1
kind: Service
metadata:
  name: {{ include "specialized-engagement.fullname" . }}
  labels:
    {{- include "specialized-engagement.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "specialized-engagement.selectorLabels" . | nindent 4 }}
