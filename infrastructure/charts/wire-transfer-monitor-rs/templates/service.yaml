apiVersion: v1
kind: Service
metadata:
  name: {{ include "wire-transfer-monitor-rs.fullname" . }}
  labels:
    {{- include "wire-transfer-monitor-rs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "wire-transfer-monitor-rs.selectorLabels" . | nindent 4 }}
