apiVersion: v1
kind: Service
metadata:
  name: {{ include "compliance-kyc-aml.fullname" . }}
  labels:
    {{- include "compliance-kyc-aml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "compliance-kyc-aml.selectorLabels" . | nindent 4 }}
