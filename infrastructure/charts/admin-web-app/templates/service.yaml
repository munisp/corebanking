apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-web-app.fullname" . }}
  labels:
    {{- include "admin-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "admin-web-app.selectorLabels" . | nindent 4 }}
