apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-ml-inference.fullname" . }}
  labels:
    {{- include "ai-ml-inference.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "ai-ml-inference.selectorLabels" . | nindent 4 }}
