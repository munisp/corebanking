apiVersion: v1
kind: Service
metadata:
  name: {{ include "client-web-app.fullname" . }}
  labels:
    {{- include "client-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "client-web-app.selectorLabels" . | nindent 4 }}
