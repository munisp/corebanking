apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-accounts.fullname" . }}
  labels:
    {{- include "core-accounts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "core-accounts.selectorLabels" . | nindent 4 }}
