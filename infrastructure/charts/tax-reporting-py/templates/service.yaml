apiVersion: v1
kind: Service
metadata:
  name: {{ include "tax-reporting-py.fullname" . }}
  labels:
    {{- include "tax-reporting-py.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "tax-reporting-py.selectorLabels" . | nindent 4 }}
