apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-web-app.fullname" . }}
  labels:
    {{- include "tenant-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tenant-web-app.selectorLabels" . | nindent 4 }}
