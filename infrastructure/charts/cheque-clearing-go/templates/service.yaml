apiVersion: v1
kind: Service
metadata:
  name: {{ include "cheque-clearing-go.fullname" . }}
  labels:
    {{- include "cheque-clearing-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "cheque-clearing-go.selectorLabels" . | nindent 4 }}
