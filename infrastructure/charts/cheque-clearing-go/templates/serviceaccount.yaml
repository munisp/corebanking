{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cheque-clearing-go.serviceAccountName" . }}
  labels:
    {{- include "cheque-clearing-go.labels" . | nindent 4 }}
{{- end }}
