apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-tenant.fullname" . }}
  labels:
    {{- include "platform-tenant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "platform-tenant.selectorLabels" . | nindent 4 }}
