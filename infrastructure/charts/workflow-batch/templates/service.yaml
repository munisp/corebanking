apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflow-batch.fullname" . }}
  labels:
    {{- include "workflow-batch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "workflow-batch.selectorLabels" . | nindent 4 }}
