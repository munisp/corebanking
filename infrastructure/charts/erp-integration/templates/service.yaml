apiVersion: v1
kind: Service
metadata:
  name: {{ include "erp-integration.fullname" . }}
  labels:
    {{- include "erp-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "erp-integration.selectorLabels" . | nindent 4 }}
