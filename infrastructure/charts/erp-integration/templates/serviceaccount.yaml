{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "erp-integration.serviceAccountName" . }}
  labels:
    {{- include "erp-integration.labels" . | nindent 4 }}
{{- end }}
