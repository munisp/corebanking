apiVersion: v1
kind: Service
metadata:
  name: {{ include "channels-voice.fullname" . }}
  labels:
    {{- include "channels-voice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "channels-voice.selectorLabels" . | nindent 4 }}
