apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflow-reporting.fullname" . }}
  labels:
    {{- include "workflow-reporting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "workflow-reporting.selectorLabels" . | nindent 4 }}
