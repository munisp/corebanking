apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-data-pipeline.fullname" . }}
  labels:
    {{- include "ai-data-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "ai-data-pipeline.selectorLabels" . | nindent 4 }}
