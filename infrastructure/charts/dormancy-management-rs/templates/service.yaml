apiVersion: v1
kind: Service
metadata:
  name: {{ include "dormancy-management-rs.fullname" . }}
  labels:
    {{- include "dormancy-management-rs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "dormancy-management-rs.selectorLabels" . | nindent 4 }}
