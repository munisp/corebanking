apiVersion: v1
kind: Service
metadata:
  name: {{ include "compliance-ui.fullname" . }}
  labels:
    {{- include "compliance-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "compliance-ui.selectorLabels" . | nindent 4 }}
