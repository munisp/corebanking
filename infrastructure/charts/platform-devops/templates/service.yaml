apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-devops.fullname" . }}
  labels:
    {{- include "platform-devops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "platform-devops.selectorLabels" . | nindent 4 }}
