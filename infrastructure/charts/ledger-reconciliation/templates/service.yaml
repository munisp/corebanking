apiVersion: v1
kind: Service
metadata:
  name: {{ include "ledger-reconciliation.fullname" . }}
  labels:
    {{- include "ledger-reconciliation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "ledger-reconciliation.selectorLabels" . | nindent 4 }}
