apiVersion: v1
kind: Service
metadata:
  name: {{ include "esusu-service.fullname" . }}
  labels:
    {{- include "esusu-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "esusu-service.selectorLabels" . | nindent 4 }}
