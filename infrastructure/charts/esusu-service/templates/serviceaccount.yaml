{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "esusu-service.serviceAccountName" . }}
  labels:
    {{- include "esusu-service.labels" . | nindent 4 }}
{{- end }}
