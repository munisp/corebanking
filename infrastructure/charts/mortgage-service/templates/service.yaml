apiVersion: v1
kind: Service
metadata:
  name: {{ include "mortgage-service.fullname" . }}
  labels:
    {{- include "mortgage-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "mortgage-service.selectorLabels" . | nindent 4 }}
