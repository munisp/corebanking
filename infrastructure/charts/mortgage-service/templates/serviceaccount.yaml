{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mortgage-service.serviceAccountName" . }}
  labels:
    {{- include "mortgage-service.labels" . | nindent 4 }}
{{- end }}
