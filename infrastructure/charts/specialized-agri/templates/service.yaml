apiVersion: v1
kind: Service
metadata:
  name: {{ include "specialized-agri.fullname" . }}
  labels:
    {{- include "specialized-agri.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "specialized-agri.selectorLabels" . | nindent 4 }}
