{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "interest-rate-engine-go.serviceAccountName" . }}
  labels:
    {{- include "interest-rate-engine-go.labels" . | nindent 4 }}
{{- end }}
