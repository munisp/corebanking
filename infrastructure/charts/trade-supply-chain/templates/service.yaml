apiVersion: v1
kind: Service
metadata:
  name: {{ include "trade-supply-chain.fullname" . }}
  labels:
    {{- include "trade-supply-chain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "trade-supply-chain.selectorLabels" . | nindent 4 }}
