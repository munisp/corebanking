apiVersion: v1
kind: Service
metadata:
  name: {{ include "channels-mobile-web.fullname" . }}
  labels:
    {{- include "channels-mobile-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "channels-mobile-web.selectorLabels" . | nindent 4 }}
