apiVersion: v1
kind: Service
metadata:
  name: {{ include "channels-agents.fullname" . }}
  labels:
    {{- include "channels-agents.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "channels-agents.selectorLabels" . | nindent 4 }}
