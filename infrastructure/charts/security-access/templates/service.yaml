apiVersion: v1
kind: Service
metadata:
  name: {{ include "security-access.fullname" . }}
  labels:
    {{- include "security-access.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "security-access.selectorLabels" . | nindent 4 }}
