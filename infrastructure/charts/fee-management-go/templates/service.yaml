apiVersion: v1
kind: Service
metadata:
  name: {{ include "fee-management-go.fullname" . }}
  labels:
    {{- include "fee-management-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "fee-management-go.selectorLabels" . | nindent 4 }}
