{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sanctions-screening-service.serviceAccountName" . }}
  labels:
    {{- include "sanctions-screening-service.labels" . | nindent 4 }}
{{- end }}
