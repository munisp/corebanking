apiVersion: v1
kind: Service
metadata:
  name: {{ include "ledger-gl.fullname" . }}
  labels:
    {{- include "ledger-gl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "ledger-gl.selectorLabels" . | nindent 4 }}
