apiVersion: v1
kind: Service
metadata:
  name: {{ include "channels-messaging.fullname" . }}
  labels:
    {{- include "channels-messaging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "channels-messaging.selectorLabels" . | nindent 4 }}
