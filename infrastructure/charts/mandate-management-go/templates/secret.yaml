apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mandate-management-go.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
