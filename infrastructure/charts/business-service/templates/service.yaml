apiVersion: v1
kind: Service
metadata:
  name: {{ include "business-service.fullname" . }}
  labels:
    {{- include "business-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "business-service.selectorLabels" . | nindent 4 }}
