apiVersion: v1
kind: Service
metadata:
  name: {{ include "specialized-islamic.fullname" . }}
  labels:
    {{- include "specialized-islamic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "specialized-islamic.selectorLabels" . | nindent 4 }}
