{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "savings-service.serviceAccountName" . }}
  labels:
    {{- include "savings-service.labels" . | nindent 4 }}
{{- end }}
