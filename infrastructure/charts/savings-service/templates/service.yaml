apiVersion: v1
kind: Service
metadata:
  name: {{ include "savings-service.fullname" . }}
  labels:
    {{- include "savings-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "savings-service.selectorLabels" . | nindent 4 }}
