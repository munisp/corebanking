apiVersion: v1
kind: Service
metadata:
  name: {{ include "swift-messaging-go.fullname" . }}
  labels:
    {{- include "swift-messaging-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "swift-messaging-go.selectorLabels" . | nindent 4 }}
