apiVersion: v1
kind: Service
metadata:
  name: {{ include "compliance-fraud.fullname" . }}
  labels:
    {{- include "compliance-fraud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "compliance-fraud.selectorLabels" . | nindent 4 }}
