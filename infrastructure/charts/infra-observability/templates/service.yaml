apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-observability.fullname" . }}
  labels:
    {{- include "infra-observability.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "infra-observability.selectorLabels" . | nindent 4 }}
