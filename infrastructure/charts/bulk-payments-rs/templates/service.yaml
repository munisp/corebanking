apiVersion: v1
kind: Service
metadata:
  name: {{ include "bulk-payments-rs.fullname" . }}
  labels:
    {{- include "bulk-payments-rs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "bulk-payments-rs.selectorLabels" . | nindent 4 }}
