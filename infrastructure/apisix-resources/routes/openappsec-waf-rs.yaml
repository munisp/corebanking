# openappsec-waf-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-openappsec-waf-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /openappsec-waf/*
      backends:
        - serviceName: security-waf
          servicePort: 9555
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/openappsec-waf/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
