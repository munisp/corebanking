# insurance-portfolio-analytics-py
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-insurance-portfolio-analytics-py-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /insurance-portfolio-analytics/*
      backends:
        - serviceName: treasury-investments
          servicePort: 9646
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/insurance-portfolio-analytics/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
