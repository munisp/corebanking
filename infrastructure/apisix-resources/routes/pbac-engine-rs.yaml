# pbac-engine-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-pbac-engine-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /pbac-engine/*
      backends:
        - serviceName: security-access
          servicePort: 9536
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/pbac-engine/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
