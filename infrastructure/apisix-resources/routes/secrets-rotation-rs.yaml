# secrets-rotation-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-secrets-rotation-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /secrets-rotation/*
      backends:
        - serviceName: workflow-operations
          servicePort: 9698
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/secrets-rotation/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
