# agri-savings-cycles-go
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-agri-savings-cycles-go-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /agri-savings-cycles/*
      backends:
        - serviceName: core-deposits
          servicePort: 9301
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/agri-savings-cycles/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
