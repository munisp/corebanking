# http2-multiplexer-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-http2-multiplexer-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /http2-multiplexer/*
      backends:
        - serviceName: infra-network
          servicePort: 9440
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/http2-multiplexer/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
