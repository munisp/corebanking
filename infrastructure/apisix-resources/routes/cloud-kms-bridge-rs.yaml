# cloud-kms-bridge-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-cloud-kms-bridge-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /cloud-kms-bridge/*
      backends:
        - serviceName: security-waf
          servicePort: 9542
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/cloud-kms-bridge/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
