# siem-exporter-py
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-siem-exporter-py-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /siem-exporter/*
      backends:
        - serviceName: infra-observability
          servicePort: 9459
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/siem-exporter/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
