# route-trie-optimizer-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-route-trie-optimizer-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /route-trie-optimizer/*
      backends:
        - serviceName: infra-network
          servicePort: 9447
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/route-trie-optimizer/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
