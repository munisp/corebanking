# db-migrations
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-db-migrations-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /db-migrations/*
      backends:
        - serviceName: infra-database
          servicePort: 9404
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/db-migrations/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
