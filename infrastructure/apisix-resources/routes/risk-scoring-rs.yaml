# risk-scoring-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-risk-scoring-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /risk-scoring/*
      backends:
        - serviceName: compliance-risk
          servicePort: 9273
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/risk-scoring/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
