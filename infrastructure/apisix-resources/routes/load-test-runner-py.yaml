# load-test-runner-py
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-load-test-runner-py-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /load-test-runner/*
      backends:
        - serviceName: workflow-operations
          servicePort: 9698
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/load-test-runner/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
