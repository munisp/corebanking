# adaptive-rate-limiter-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-adaptive-rate-limiter-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /adaptive-rate-limiter/*
      backends:
        - serviceName: infra-network
          servicePort: 9432
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/adaptive-rate-limiter/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
