# gnn-fraud-detection-py
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-gnn-fraud-detection-py-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /gnn-fraud-detection/*
      backends:
        - serviceName: compliance-fraud
          servicePort: 9204
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/gnn-fraud-detection/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
