# ddos-protection-go
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-ddos-protection-go-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /ddos-protection/*
      backends:
        - serviceName: security-waf
          servicePort: 9544
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/ddos-protection/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
