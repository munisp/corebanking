# crossborder-agri-trade-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-crossborder-agri-trade-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /crossborder-agri-trade/*
      backends:
        - serviceName: specialized-agri
          servicePort: 9576
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/crossborder-agri-trade/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
