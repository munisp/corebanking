# sms-alert-notification-py
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-sms-alert-notification-py-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /sms-alert-notification/*
      backends:
        - serviceName: channels-messaging
          servicePort: 9151
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/sms-alert-notification/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
