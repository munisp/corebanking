# risk-based-approach-py
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-risk-based-approach-py-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /risk-based-approach/*
      backends:
        - serviceName: compliance-risk
          servicePort: 9271
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/risk-based-approach/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
