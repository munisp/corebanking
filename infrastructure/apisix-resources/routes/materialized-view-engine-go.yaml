# materialized-view-engine-go
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-materialized-view-engine-go-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /materialized-view-engine/*
      backends:
        - serviceName: ai-data-pipeline
          servicePort: 9104
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/materialized-view-engine/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
