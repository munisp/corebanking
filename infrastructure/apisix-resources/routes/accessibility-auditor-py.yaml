# accessibility-auditor-py
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-accessibility-auditor-py-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /accessibility-auditor/*
      backends:
        - serviceName: workflow-operations
          servicePort: 9671
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/accessibility-auditor/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
