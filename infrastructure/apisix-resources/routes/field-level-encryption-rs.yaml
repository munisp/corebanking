# field-level-encryption-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-field-level-encryption-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /field-level-encryption/*
      backends:
        - serviceName: security-waf
          servicePort: 9548
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/field-level-encryption/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
