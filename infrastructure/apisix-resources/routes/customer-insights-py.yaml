# customer-insights-py
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-customer-insights-py-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /customer-insights/*
      backends:
        - serviceName: ai-ml-inference
          servicePort: 9130
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/customer-insights/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
