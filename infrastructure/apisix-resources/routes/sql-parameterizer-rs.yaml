# sql-parameterizer-rs
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-sql-parameterizer-rs-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /sql-parameterizer/*
      backends:
        - serviceName: workflow-operations
          servicePort: 9703
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/sql-parameterizer/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
