# nirsal-agro-geocoop-go
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-nirsal-agro-geocoop-go-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /nirsal-agro-geocoop/*
      backends:
        - serviceName: specialized-agri
          servicePort: 9585
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/nirsal-agro-geocoop/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
