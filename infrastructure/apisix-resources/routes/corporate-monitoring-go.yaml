# corporate-monitoring-go
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54link-dev-corporate-monitoring-go-route
  namespace: 54link-dev
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54link-dev.upi.dev
        paths:
          - /corporate-monitoring/*
      backends:
        - serviceName: infra-observability
          servicePort: 9453
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/corporate-monitoring/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
