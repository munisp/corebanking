---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-detection-rs
  namespace: 54bank
  labels:
    app: fraud-detection-rs
    tier: tier1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fraud-detection-rs
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: fraud-detection-rs
    spec:
      serviceAccountName: fraud-detection-rs
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: fraud-detection-rs
          image: 54bank/fraud-detection-rs:latest
          ports:
            - containerPort: 8112
              protocol: TCP
          env:
            - name: PORT
              value: "8112"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: url
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: redis-config
                  key: url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
            - name: TLS_ENABLED
              value: "true"
            - name: TLS_CERT_PATH
              value: "/etc/54bank/certs/service.crt"
            - name: TLS_KEY_PATH
              value: "/etc/54bank/certs/service.key"
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /livez
              port: 8112
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8112
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 2
          volumeMounts:
            - name: tls-certs
              mountPath: /etc/54bank/certs
              readOnly: true
      volumes:
        - name: tls-certs
          secret:
            secretName: fraud-detection-rs-tls
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-rs
  namespace: 54bank
spec:
  selector:
    app: fraud-detection-rs
  ports:
    - port: 8112
      targetPort: 8112
      protocol: TCP
  type: ClusterIP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: fraud-detection-rs
  namespace: 54bank
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: fraud-detection-rs
