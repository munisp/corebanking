apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-routing-rs
  labels:
    app: payment-routing-rs
    tier: platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-routing-rs
  template:
    metadata:
      labels:
        app: payment-routing-rs
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z postgres 5432; do sleep 2; done"]
        - name: wait-for-kafka
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z kafka 9092; do sleep 2; done"]
      containers:
        - name: payment-routing-rs
          image: 54bank/payment-routing-rs:latest
          ports:
            - containerPort: 9051
          env:
            - name: PORT
              value: "9051"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: url
            - name: KAFKA_BROKERS
              value: "kafka:9092"
            - name: REDIS_URL
              value: "redis://redis:6379"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9051
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 9051
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: payment-routing-rs
spec:
  selector:
    app: payment-routing-rs
  ports:
    - port: 9051
      targetPort: 9051
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: payment-routing-rs-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: payment-routing-rs
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: payment-routing-rs-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: payment-routing-rs
