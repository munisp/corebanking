apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-verification-py
  labels:
    app: document-verification-py
    tier: platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: document-verification-py
  template:
    metadata:
      labels:
        app: document-verification-py
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z postgres 5432; do sleep 2; done"]
        - name: wait-for-kafka
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z kafka 9092; do sleep 2; done"]
      containers:
        - name: document-verification-py
          image: 54bank/document-verification-py:latest
          ports:
            - containerPort: 9042
          env:
            - name: PORT
              value: "9042"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: url
            - name: KAFKA_BROKERS
              value: "kafka:9092"
            - name: REDIS_URL
              value: "redis://redis:6379"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9042
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 9042
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: document-verification-py
spec:
  selector:
    app: document-verification-py
  ports:
    - port: 9042
      targetPort: 9042
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: document-verification-py-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: document-verification-py
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: document-verification-py-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: document-verification-py
