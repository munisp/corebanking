[package]
name = "rust-reconciliation-auditor"
version = "0.1.0"
edition = "2021"
description = "Rust utility for deterministic reconciliation discrepancy evaluation"

[dependencies]
clap = { version = "=4.4.18", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
