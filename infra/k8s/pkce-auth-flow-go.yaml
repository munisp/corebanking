---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pkce-auth-flow-go
  namespace: 54bank
  labels:
    app: pkce-auth-flow-go
    tier: tier3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pkce-auth-flow-go
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: pkce-auth-flow-go
    spec:
      serviceAccountName: pkce-auth-flow-go
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: pkce-auth-flow-go
          image: 54bank/pkce-auth-flow-go:latest
          ports:
            - containerPort: 8503
              protocol: TCP
          env:
            - name: PORT
              value: "8503"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: url
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: redis-config
                  key: url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
            - name: TLS_ENABLED
              value: "true"
            - name: TLS_CERT_PATH
              value: "/etc/54bank/certs/service.crt"
            - name: TLS_KEY_PATH
              value: "/etc/54bank/certs/service.key"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /livez
              port: 8503
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8503
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 2
          volumeMounts:
            - name: tls-certs
              mountPath: /etc/54bank/certs
              readOnly: true
      volumes:
        - name: tls-certs
          secret:
            secretName: pkce-auth-flow-go-tls
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: pkce-auth-flow-go
  namespace: 54bank
spec:
  selector:
    app: pkce-auth-flow-go
  ports:
    - port: 8503
      targetPort: 8503
      protocol: TCP
  type: ClusterIP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: pkce-auth-flow-go
  namespace: 54bank
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: pkce-auth-flow-go
