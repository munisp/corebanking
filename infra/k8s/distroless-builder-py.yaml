---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: distroless-builder-py
  namespace: 54bank
  labels:
    app: distroless-builder-py
    tier: tier3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: distroless-builder-py
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: distroless-builder-py
    spec:
      serviceAccountName: distroless-builder-py
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: distroless-builder-py
          image: 54bank/distroless-builder-py:latest
          ports:
            - containerPort: 8564
              protocol: TCP
          env:
            - name: PORT
              value: "8564"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: url
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: redis-config
                  key: url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
            - name: TLS_ENABLED
              value: "true"
            - name: TLS_CERT_PATH
              value: "/etc/54bank/certs/service.crt"
            - name: TLS_KEY_PATH
              value: "/etc/54bank/certs/service.key"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /livez
              port: 8564
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8564
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 2
          volumeMounts:
            - name: tls-certs
              mountPath: /etc/54bank/certs
              readOnly: true
      volumes:
        - name: tls-certs
          secret:
            secretName: distroless-builder-py-tls
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: distroless-builder-py
  namespace: 54bank
spec:
  selector:
    app: distroless-builder-py
  ports:
    - port: 8564
      targetPort: 8564
      protocol: TCP
  type: ClusterIP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: distroless-builder-py
  namespace: 54bank
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: distroless-builder-py
