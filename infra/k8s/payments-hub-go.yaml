---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-hub-go
  namespace: 54bank
  labels:
    app: payments-hub-go
    tier: tier1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payments-hub-go
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: payments-hub-go
    spec:
      serviceAccountName: payments-hub-go
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: payments-hub-go
          image: 54bank/payments-hub-go:latest
          ports:
            - containerPort: 8107
              protocol: TCP
          env:
            - name: PORT
              value: "8107"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: url
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: redis-config
                  key: url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
            - name: TLS_ENABLED
              value: "true"
            - name: TLS_CERT_PATH
              value: "/etc/54bank/certs/service.crt"
            - name: TLS_KEY_PATH
              value: "/etc/54bank/certs/service.key"
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /livez
              port: 8107
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8107
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 2
          volumeMounts:
            - name: tls-certs
              mountPath: /etc/54bank/certs
              readOnly: true
      volumes:
        - name: tls-certs
          secret:
            secretName: payments-hub-go-tls
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: payments-hub-go
  namespace: 54bank
spec:
  selector:
    app: payments-hub-go
  ports:
    - port: 8107
      targetPort: 8107
      protocol: TCP
  type: ClusterIP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: payments-hub-go
  namespace: 54bank
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: payments-hub-go
