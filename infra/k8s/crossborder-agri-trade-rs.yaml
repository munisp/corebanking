---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crossborder-agri-trade-rs
  namespace: 54bank
  labels:
    app: crossborder-agri-trade-rs
    tier: tier3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crossborder-agri-trade-rs
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: crossborder-agri-trade-rs
    spec:
      serviceAccountName: crossborder-agri-trade-rs
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: crossborder-agri-trade-rs
          image: 54bank/crossborder-agri-trade-rs:latest
          ports:
            - containerPort: 8619
              protocol: TCP
          env:
            - name: PORT
              value: "8619"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: url
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: redis-config
                  key: url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
            - name: TLS_ENABLED
              value: "true"
            - name: TLS_CERT_PATH
              value: "/etc/54bank/certs/service.crt"
            - name: TLS_KEY_PATH
              value: "/etc/54bank/certs/service.key"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /livez
              port: 8619
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8619
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 2
          volumeMounts:
            - name: tls-certs
              mountPath: /etc/54bank/certs
              readOnly: true
      volumes:
        - name: tls-certs
          secret:
            secretName: crossborder-agri-trade-rs-tls
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: crossborder-agri-trade-rs
  namespace: 54bank
spec:
  selector:
    app: crossborder-agri-trade-rs
  ports:
    - port: 8619
      targetPort: 8619
      protocol: TCP
  type: ClusterIP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: crossborder-agri-trade-rs
  namespace: 54bank
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: crossborder-agri-trade-rs
