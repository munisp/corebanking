apiVersion: v1
kind: Service
metadata:
  name: {{ include "54bank.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ include "54bank.name" . }}
