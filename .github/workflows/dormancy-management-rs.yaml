name: Build and Deploy Dormancy Management Service

on:
  push:
    branches:
      - main
    paths:
      - 'services/dormancy-management-rs/**'
      - 'infrastructure/charts/dormancy-management-rs/**'
      - 'infrastructure/apisix-resources/routes/dormancy-management-rs.yaml'
      - '.github/workflows/dormancy-management-rs.yaml'

env:
  IMAGE: registry.digitalocean.com/talentgraph-auth/54link-dev-dormancy-management-rs
  TAG: "0.0.1"
  CHART: infrastructure/charts/dormancy-management-rs
  RELEASE: dormancy-management-rs
  NAMESPACE: 54link-dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: docker
          password: ${{ secrets.DO_API_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./services/dormancy-management-rs
          file: ./services/dormancy-management-rs/Dockerfile
          push: true
          tags: ${{ env.IMAGE }}:${{ env.TAG }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DO_API_TOKEN }}

      - name: Save kubeconfig
        run: |
          mkdir -p "$HOME/.kube"
          printf '%s' '${{ secrets.KUBECONFIG }}' > "$HOME/.kube/config"
          chmod 600 "$HOME/.kube/config"

      - name: Install Helm
        uses: azure/setup-helm@v3

      - name: Deploy via Helm
        run: |
          helm upgrade --install ${{ env.RELEASE }} ./${{ env.CHART }} \
            --namespace ${{ env.NAMESPACE }} \
            --set image.tag=${{ env.TAG }} \
            --wait \
            --timeout 5m

      - name: Apply APISIX route
        run: |
          kubectl apply -f infrastructure/apisix-resources/routes/dormancy-management-rs.yaml \
            -n ${{ env.NAMESPACE }}
