name: Build and Deploy Sanctions Screening Service

on:
  push:
    branches:
      - main
    paths:
      - 'services/sanctions-screening-service/**'
      - 'infrastructure/charts/sanctions-screening-service/**'
      - '.github/workflows/sanctions-screening-service.yaml'

env:
  IMAGE: registry.digitalocean.com/talentgraph-auth/54link-dev-sanctions-screening-service
  TAG: "0.0.1"
  CHART: infrastructure/charts/sanctions-screening-service
  RELEASE: sanctions-screening-service
  NAMESPACE: 54link-dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: docker
          password: ${{ secrets.DO_API_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./services/sanctions-screening-service
          file: ./services/sanctions-screening-service/Dockerfile
          push: true
          tags: ${{ env.IMAGE }}:${{ env.TAG }}

