name: Build and Deploy Mandate Management Service

on:
  push:
    branches:
      - main
      - development
    paths:
      - 'services/mandate-management-go/**'
      - 'infrastructure/charts/mandate-management-go/**'
      - 'infrastructure/apisix-resources/routes/mandate-management-go.yaml'
      - '.github/workflows/mandate-management-go.yaml'

env:
  IMAGE: registry.digitalocean.com/talentgraph-auth/54link-dev-mandate-management-go
  TAG: "0.0.2"
  CHART: infrastructure/charts/mandate-management-go
  RELEASE: mandate-management-go
  NAMESPACE: 54link-dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: docker
          password: ${{ secrets.DO_API_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./services/mandate-management-go
          file: ./services/mandate-management-go/Dockerfile
          push: true
          tags: ${{ env.IMAGE }}:${{ env.TAG }}

