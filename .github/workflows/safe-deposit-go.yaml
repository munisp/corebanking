name: Build and Deploy Safe Deposit Service

on:
  push:
    branches:
      - main
      - development
    paths:
      - 'services/safe-deposit-go/**'
      - 'infrastructure/charts/safe-deposit-go/**'
      - 'infrastructure/apisix-resources/routes/safe-deposit-go.yaml'
      - '.github/workflows/safe-deposit-go.yaml'

env:
  IMAGE: registry.digitalocean.com/talentgraph-auth/54link-dev-safe-deposit-go
  TAG: "0.0.1"
  CHART: infrastructure/charts/safe-deposit-go
  RELEASE: safe-deposit-go
  NAMESPACE: 54link-dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: docker
          password: ${{ secrets.DO_API_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./services/safe-deposit-go
          file: ./services/safe-deposit-go/Dockerfile
          push: true
          tags: ${{ env.IMAGE }}:${{ env.TAG }}

