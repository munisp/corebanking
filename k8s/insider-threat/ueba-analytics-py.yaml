apiVersion: apps/v1
kind: Deployment
metadata:
  name: ueba-analytics-py
  namespace: 54bank-security
  labels:
    app: ueba-analytics-py
    domain: security
    tier: insider-threat
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ueba-analytics-py
  template:
    metadata:
      labels:
        app: ueba-analytics-py
        domain: security
        tier: insider-threat
    spec:
      restartPolicy: Always
      initContainers:
        - name: wait-for-kafka
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z kafka-bootstrap.kafka.svc.cluster.local 9092; do echo waiting for kafka; sleep 2; done']
        - name: wait-for-postgres
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z postgres-primary.database.svc.cluster.local 5432; do echo waiting for postgres; sleep 2; done']
        - name: wait-for-redis
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z redis-cluster.cache.svc.cluster.local 6379; do echo waiting for redis; sleep 2; done']
      containers:
        - name: ueba-analytics-py
          image: 54bank/ueba-analytics-py:latest
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: PORT
              value: "8080"
            - name: SERVICE_NAME
              value: "ueba-analytics-py"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: security-db-credentials
                  key: DATABASE_URL
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: kafka-config
                  key: KAFKA_BROKERS
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: redis-config
                  key: REDIS_URL
          livenessProbe:
            httpGet:
              path: /livez
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: ueba-analytics-py
  namespace: 54bank-security
  labels:
    app: ueba-analytics-py
spec:
  selector:
    app: ueba-analytics-py
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ueba-analytics-py-pdb
  namespace: 54bank-security
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: ueba-analytics-py
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ueba-analytics-py-hpa
  namespace: 54bank-security
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ueba-analytics-py
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
